CMAKE_MINIMUM_REQUIRED(VERSION 3.5.1)

PROJECT(lsmmap)

SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -Wpedantic -Wall")

SET(EXECUTABLE_NAME lsmmap)

INCLUDE_DIRECTORIES(include)
ADD_SUBDIRECTORY(lib)

ADD_EXECUTABLE(${EXECUTABLE_NAME}
  ${LSMMAP_HEADERS}
  ${LSMMAP_SOURCES}
)
